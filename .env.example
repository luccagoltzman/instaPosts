# Desenvolvimento local: chave exposta no frontend (só use em .env, nunca commite)
# Obtenha em: https://rapidapi.com → API "Instagram Scraper Stable API"
VITE_RAPIDAPI_KEY=

# Host da API (opcional; padrão: instagram-scraper-stable-api.p.rapidapi.com)
# VITE_RAPIDAPI_HOST=instagram-scraper-stable-api.p.rapidapi.com

# Produção (Vercel): configure RAPIDAPI_KEY nas Environment Variables do projeto.
# A chave fica apenas no servidor (proxy em /api/instagram/*).
